{
  "name": "pnpm-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm run clean && pnpm -r run build",
    "build:alice": "pnpm -F alice... run build",
    "clean": "rimraf packages/**/**/dist",
    "turbo:watch": "turbo run watch",
    "turbo:build": "pnpm run clean && turbo run build"
  },
  "keywords": [],
  "author": "colgin",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.27.0",
    "tsup": "^6.4.0",
    "turbo": "^1.6.3",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
